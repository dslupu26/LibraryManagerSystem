@using Bussiness.Common
@inherits LayoutComponentBase
@* @inject MessageService messageService *@

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
      @*   <div class="top-row px-4">
            @if (!string.IsNullOrEmpty(messageService.Message))
            {
                <span class="@messageService.MessageType">@messageService.Message</span>
            }
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div> *@

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

@code {
    
    // protected override void OnInitialized()
    // {
    //     this.messageService.OnMessageChanged += StateHasChanged;
    //     Console.WriteLine("MainLayout subscribed to MessageService.OnMessageChanged");
    // }

    // public void Dispose()
    // {
    //     messageService.OnMessageChanged -= StateHasChanged;
    // }
}
